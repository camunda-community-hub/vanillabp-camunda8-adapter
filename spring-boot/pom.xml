<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.camunda.community.vanillabp</groupId>
    <artifactId>camunda8-adapter</artifactId>
    <version>1.10.0-SNAPSHOT</version>
  </parent>
  
  <artifactId>camunda8-spring-boot-adapter</artifactId>
  <name>VanillaBP SPI adapter for Camunda 8.x for Spring Boot</name>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
  </properties>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <configuration>
            <source>17</source>
            <target>17</target>
          </configuration>
        </plugin>
      </plugins>
    </pluginManagement>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-source-plugin</artifactId>
      </plugin>
    </plugins>
  </build>

  <dependencies>
    <dependency>
      <groupId>io.vanillabp</groupId>
      <artifactId>spring-boot-support</artifactId>
      <version>1.4.0</version>
    </dependency>
    <dependency>
      <groupId>io.camunda</groupId>
      <!--
        Deliberately kept on the 8.8 line, and deliberately the -4-starter rather than the default one:
        in 8.8.x the default starter targets Spring Boot 3 and only camunda-spring-boot-4-starter targets
        Spring Boot 4. Note the naming is inverted in 8.9.x, where the default starter is the Boot 4 one.

        Why 8.8 and not 8.9: Camunda only guarantees compatibility between a client and a cluster of the
        same version, and the supported upgrade order is cluster first, then client. A newer client may
        call APIs an older cluster does not offer yet. Cluster upgrades take time in enterprises, so this
        adapter must not force one. Upgrading the Camunda line is a separate step, independent of the
        Spring Boot migration.
      -->
      <artifactId>camunda-spring-boot-4-starter</artifactId>
      <version>8.8.33</version>
    </dependency>
    <dependency>
      <groupId>org.springframework</groupId>
      <artifactId>spring-tx</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework</groupId>
      <artifactId>spring-orm</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.data</groupId>
      <artifactId>spring-data-jpa</artifactId>
      <optional>true</optional>
    </dependency>
    <dependency>
      <groupId>org.springframework.data</groupId>
      <artifactId>spring-data-mongodb</artifactId>
      <optional>true</optional>
    </dependency>
    <dependency>
      <groupId>org.springframework.retry</groupId>
      <artifactId>spring-retry</artifactId>
      <!--
        Not managed by the Spring Boot BOM, and there is no Spring 7 release: 2.0.13 is built against
        spring-context 6.2. Pinned deliberately; see the notes in prompts/T09-*.md.
      -->
      <version>2.0.13</version>
    </dependency>
    <!--
      spring-boot-starter-aop no longer exists in Spring Boot 4 - its last release is 3.5.16. It used to
      aggregate spring-boot-starter, spring-aop and aspectjweaver, so those are declared directly. Both
      versions come from the BOM.
    -->
    <dependency>
      <groupId>org.springframework</groupId>
      <artifactId>spring-aop</artifactId>
    </dependency>
    <dependency>
      <groupId>org.aspectj</groupId>
      <artifactId>aspectjweaver</artifactId>
    </dependency>
    <dependency>
      <groupId>jakarta.persistence</groupId>
      <artifactId>jakarta.persistence-api</artifactId>
    </dependency>
    <dependency>
        <groupId>org.junit.jupiter</groupId>
        <artifactId>junit-jupiter-engine</artifactId>
        <scope>test</scope>
    </dependency>
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-test</artifactId>
        <scope>test</scope>
    </dependency>
  </dependencies>
  
  <licenses>
    <license>
      <name>Apache License, Version 2.0</name>
      <url>http://www.apache.org/licenses/LICENSE-2.0.txt</url>
      <distribution>repo</distribution>
    </license>
  </licenses>

  <developers>
    <developer>
      <id>VanillaBP</id>
      <organization>Phactum Softwareentwicklung GmbH</organization>
      <organizationUrl>https://www.phactum.at</organizationUrl>
    </developer>
  </developers>
</project>
